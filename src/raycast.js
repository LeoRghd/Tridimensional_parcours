const raycast = function (scene) {
  if (BABYLON.PointerEventTypes.POINTERMOVE) {
    console.log('move');
  }
  // scene.pick(scene.pointerX, scene.pointerY)
  // console.log(scene.pointerX, scene.pointerY)
}
